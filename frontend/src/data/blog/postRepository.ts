/**
 * Single entry for listing and resolving posts. Swap exports to a Firebase/CMS
 * implementation later while keeping pages and components unchanged.
 */
export { getAllPosts, getAllTags, getPostBySlug } from "./localPosts";
